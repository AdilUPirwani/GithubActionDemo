name: SonarCloud
on:
  workflow_dispatch:
    inputs:
      codecoveragetool:
        type: choice
        description: Code Coverage Tool
        required: true
        default: dotnet-coverage
        options:
          - dotnet-coverage
          - dotCover
          - coverlet
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened]
    
jobs:
  build:
    name: Build and analyze
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
        with: 
            fetch_depth: 0
            
      - name: Set code coverage tool output
        run: |
          echo ${{ github.event.inputs.codecoveragetool }}
     
